<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# PERSISTENT REPOSITORY RULES
1. **Never restart, recreate, or delete the Next.js project layout** (no `create-next-app` commands).
2. **Inspect before modifying** (verify all existing files before making edits).
3. **Preserve preserved references**: Do not edit `reference_style.css`, `reference_index.html`, `reference_script.js`, or `reference_chromatic-waves.js`.
4. **Case Study Truth**: Never invent ROAS, revenue numbers, percentage improvements, sales volume, timelines, or client quotes. Mapped metrics must be strictly verified.
5. **Aesthetics & Priority**: Maintain dark/lime themes with editorial serif typography. Priority: Conversion clarity > decorative complexity.
6. **Compile Validation**: Run `npm run build` to verify standard compliance before claiming completion.
7. **Transition Updates**: Keep `PROGRESS.md` and `AI_HANDOFF.md` updated at meaningful phase boundaries.
8. **Phase Scope**: Complete the active phase tasks only. Do not begin subsequent phases without explicit user approval.

